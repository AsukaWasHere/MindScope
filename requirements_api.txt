# ─────────────────────────────────────────────────────────────────────
# requirements_api.txt
# FastAPI deployment — only what the server needs at RUNTIME
# No training dependencies (torch, sentence-transformers etc.) included
# unless you're serving BERT. See comments below.
# ─────────────────────────────────────────────────────────────────────

# ── Core data ─────────────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# ── NLP preprocessing (used by text_processor.py at inference) ────────
nltk>=3.8.0
spacy>=3.7.0

# ── Classical ML model (pick whichever you trained) ───────────────────
lightgbm>=4.0.0
# xgboost>=2.0.0       # uncomment if serving XGBoost

# ── Sentiment analysis ────────────────────────────────────────────────
vaderSentiment>=3.3.2

# ── API server ────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9    # needed for FastAPI form support

# ── BERT serving (uncomment ONLY if API_CONFIG model_backend = "bert") ─
# torch>=2.0.0
# transformers>=4.38.0
